<link rel="stylesheet" href="/css/login.css">

<body>
    <div class="container" style="margin:2em">
        <img src="/img/logo.svg" alt="">
        <form action="/register" enctype="multipart/form-data" method="POST">
            <h1 style="font-size: 1.5em;">Dodaj korisnika</h1>
            <!-- <div class="porka">

                <% if(infoSubmitObj !='' ){%>
                    <%= infoSubmitObj %>
                        <% } %>
                            <% if(infoErrorsObj !='' ){%>
                                <%= infoErrorsObj %>
                                    <% } %>
            </div> -->
            <div class="input">
                <label for="email">Email</label>
                <input type="email" placeholder="email" name="email" id="email">
            </div>
            <div class="input">
                <label for="username">Username</label>
                <input type="text" placeholder="username" name="username" id="username" required>
            </div>
            <div class="input">
                <label for="password">Password</label>
                <input type="password" placeholder="password" name="password" id="password" min="6" required>
            </div>
            <div class="input">
                <label for="role">Rola</label>
                <select name="role" id="role" style="padding: 1em;">
                    <option value="promoter" selected>promoter</option>
                    <option value="organizator">organizator</option>
                </select>
                <!-- <input type="text" placeholder="organizator/promoter" name="role" id="role" required> -->
            </div>
            <button type="submit">DODAJ</button>
        </form>
    </div>
</body>

</html>